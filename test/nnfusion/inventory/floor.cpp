// Microsoft (c) 2019
/**
 * \brief Unit tests for ir::Floor
 * \author generated by script
 */

#include "nnfusion/core/operators/floor.hpp"
#include "../test_util/common.hpp"
#include "nnfusion/core/operators/parameter.hpp"

using namespace ngraph;

namespace nnfusion
{
    namespace test
    {
        template <typename T, size_t N>
        using NDArray = nnfusion::test::NDArray<T, N>;
    }

    namespace inventory
    {
        template <>
        shared_ptr<op::Floor> create_object<op::Floor, float>(int option)
        {
            switch (option)
            {
            case 0:
            {
                Shape shape{2, 2};
                auto A = make_shared<op::Parameter>(element::f32, shape);
                return make_shared<op::Floor>(A);
            }
            default: return nullptr;
            }
        }

        template <>
        vector<float> generate_input<op::Floor, float>(int option)
        {
            switch (option)
            {
            case 0:
            {
                vector<float> a = vector<float>{-2.5f, -2.0f, 0.3f, 4.8f};
                auto return_vector = vector<float>();
                return_vector.insert(return_vector.end(), a.begin(), a.end());
                return return_vector;
            }
            default: return vector<float>();
            }
        }

        template <>
        vector<float> generate_output<op::Floor, float>(int option)
        {
            switch (option)
            {
            case 0:
            {
                vector<float> result = vector<float>{-3.0f, -2.0f, 0.0f, 4.0f};
                auto return_vector = vector<float>();
                return_vector.insert(return_vector.end(), result.begin(), result.end());
                return return_vector;
            }
            default: return vector<float>();
            }
        }
    }
}